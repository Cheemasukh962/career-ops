Intern Drop — the only scanner. Category Slack Bots read intern-drop/CHIEF.txt for the team shape.

cd ~/intern-drop/apply
git pull --ff-only
node scripts/intern-drop.mjs --full

Then read intern-drop/state/latest.json and intern-drop/state/slack-intern-*.md
Handoff non-empty channel files to the matching Slack Bot. Empty file = post nothing.
No job cap. Split Slack messages if length-limited. Never apply. Never git commit.
If portals.yml is missing, copy templates/portals-intern.yml (not portals.example.yml).
